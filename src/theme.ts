import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  palette: {
    primary: {
      main: '#257CA3',
      light: '#4A9ABE',
    },
    secondary: {
      main: '#3f51b5'
    },
    error: {
      main: '#f44336'
    },
    
  }
})

export default theme
